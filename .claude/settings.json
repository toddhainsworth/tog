{
  "project": {
    "testing": {
      "policy": "selective",
      "commandTests": false,
      "description": "CLI commands do NOT require test coverage - focus on core logic in lib/ files",
      "exclusions": [
        "src/commands/*"
      ]
    }
  },
  "hooks": [
    {
      "matcher": "Task",
      "hooks": [
        {
          "type": "validation",
          "description": "Suppress command test requirements for oclif CLI project",
          "rules": [
            "Commands in src/commands/ do not require tests",
            "This is an oclif CLI tool where command tests are intentionally excluded",
            "Focus testing on lib/ files only"
          ]
        }
      ]
    }
  ],
  "permissions": {
    "allow": [
      "Read(test/lib/**)",
      "Write(test/lib/**)"
    ],
    "deny": [
      "Write(test/commands/**)"
    ]
  }
}